class ScreenConstant {
  // final BuildContext context;
  // const ScreenConstant(this.context);
  // static final double screen_height = MediaQuery.of(context).size.height;
  // static final double screen_width = MediaQuery.of(context).size.width;
}
